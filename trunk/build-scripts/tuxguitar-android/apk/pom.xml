<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                      http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<parent>
		<artifactId>tuxguitar-pom</artifactId>
		<groupId>org.herac.tuxguitar</groupId>
		<version>1.3-SNAPSHOT</version>
		<relativePath>../../../</relativePath>
	</parent>

	<modelVersion>4.0.0</modelVersion>
	<artifactId>tuxguitar-android-apk</artifactId>
	<packaging>apk</packaging>
	<name>${project.artifactId}</name>

	<properties>
		<project.assets.directory>${project.build.directory}/assets</project.assets.directory>
	</properties>
	
	<dependencies>
		<dependency>
			<groupId>org.herac.tuxguitar</groupId>
			<artifactId>tuxguitar-android-lib</artifactId>
			<version>${project.version}</version>
			<type>aar</type>
			<scope>compile</scope>
		</dependency>
		<dependency>
			<groupId>org.herac.tuxguitar</groupId>
			<artifactId>tuxguitar-compat</artifactId>
			<version>${project.version}</version>
			<scope>provided</scope>
			<type>jar</type>
		</dependency>
		<dependency>
			<groupId>org.herac.tuxguitar</groupId>
			<artifactId>tuxguitar-gtp</artifactId>
			<version>${project.version}</version>
			<scope>provided</scope>
			<type>jar</type>
		</dependency>
	</dependencies>
	
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<version>2.4</version>
				<executions>
					<execution>
						<id>copy-libs</id>
						<phase>validate</phase>
						<goals>
							<goal>copy</goal>
						</goals>
						<configuration>
							<artifactItems>
								<artifactItem>
									<groupId>${project.groupId}</groupId>
									<artifactId>tuxguitar-compat</artifactId>
									<destFileName>tuxguitar-compat.jar</destFileName>
									<outputDirectory>${project.assets.directory}/plugins</outputDirectory>
								</artifactItem>
								<artifactItem>
									<groupId>${project.groupId}</groupId>
									<artifactId>tuxguitar-gtp</artifactId>
									<destFileName>tuxguitar-gtp.jar</destFileName>
									<outputDirectory>${project.assets.directory}/plugins</outputDirectory>
								</artifactItem>
							</artifactItems>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-antrun-plugin</artifactId>
				<version>1.7</version>
				<executions>
					<execution>
						<id>dex-libs</id>
						<phase>validate</phase>
						<configuration>
							<target name="dex-libs">
								<exec dir="${project.assets.directory}" executable="dx" failonerror="true" >
									<arg value="--dex" />
									<arg value="--keep-classes" />
									<arg value="--output=plugins/tuxguitar-compat.jar" />
									<arg value="plugins/tuxguitar-compat.jar" />
								</exec>
								
								<exec dir="${project.assets.directory}" executable="dx" failonerror="true" >
									<arg value="--dex" />
									<arg value="--keep-classes" />
									<arg value="--output=plugins/tuxguitar-gtp.jar" />
									<arg value="plugins/tuxguitar-gtp.jar" />
								</exec>
							</target>
						</configuration>
						<goals>
							<goal>run</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>com.simpligility.maven.plugins</groupId>
				<artifactId>android-maven-plugin</artifactId>
				<version>4.3.0</version>
				<extensions>true</extensions>
				<configuration>
					<sdk>
						<platform>15</platform>
					</sdk>
					<assetsDirectory>${project.assets.directory}</assetsDirectory>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<repositories>
		<repository>
			<id>android-support</id>
			<url>file://${env.ANDROID_HOME}/extras/android/m2repository</url>
		</repository>
	</repositories>
</project>
